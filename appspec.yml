version: 0.0
os: linux
files:
  - source: /
    destination: /root
    overwrite: true
hooks:
  BeforeInstall:
    - location: scripts\docker.sh
      timeout: 200
      runas: root
  AfterInstall:
    - location: scripts\after.sh
      timeout: 200
      runas: root
